{
  "buildCommand": "cp -r ../svg/* public/svg/ && mkdir -p public/webp && cp -r ../webp/* public/webp/ && npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "npm install"
}
